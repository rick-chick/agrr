<!DOCTYPE html>
<html>
  <head>
    <title>Admin - <%= content_for?(:title) ? yield(:title) : "AGRR" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="turbo-visit-control" content="reload">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bundled CSS (Leaflet など npm パッケージ) - esbuild でビルド -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    
    <!-- Core Design System - 必ず最初に読み込む -->
    <%= render_core_stylesheets %>
    
    <!-- Utility CSS classes -->
    <%= render_utility_stylesheets %>
    
    <!-- Components -->
    <%= render_component_stylesheets %>
    
    <!-- Layout-specific styles -->
    <%= render_feature_stylesheets(features: ["layouts/admin"]) %>
    
    <!-- Bundled JavaScript (includes Leaflet) -->
    <%= render_common_javascripts(include_shared_systems: true) %>
  </head>

  <body <%= raw (js_i18n_data.merge(js_i18n_templates)).map { |k, v| "data-#{k.to_s.dasherize}=\"#{ERB::Util.html_escape(v)}\"" }.join(' ') %>>
    <%= render "shared/navbar" %>
    
    <div class="admin-container">
      <% if notice %>
        <div class="alert alert-success" role="status" aria-live="polite"><%= notice %></div>
      <% end %>
      
      <% if alert %>
        <div class="alert alert-danger" role="alert" aria-live="assertive"><%= alert %></div>
      <% end %>
      
      <%= yield %>
    </div>
    
    <%= yield :javascripts %>
    
    <!-- Development: Client-side logging to server -->
    <% if Rails.env.development? %>
      <%= javascript_include_tag "client_logger", "data-turbo-track": "reload", defer: true %>
    <% end %>
  </body>
</html>



