<!DOCTYPE html>
<html lang="ja">
<head>
  <%= render "shared/meta_tags" %>
  
  <!-- Bundled CSS (Leaflet など npm パッケージ) - esbuild でビルド -->
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  
  <!-- Core Design System - 必ず最初に読み込む -->
  <%= render_core_stylesheets %>
  
  <!-- Utility CSS classes -->
  <%= render_utility_stylesheets %>
  
  <!-- Components -->
  <%= render_component_stylesheets %>
  
  <!-- Feature-specific styles -->
  <%= render_feature_stylesheets(features: [
    "features/public-plans",
    "features/optimizing",
    "features/results",
    "public_plans_results",
    "features/fields-crops",
    "features/custom_gantt_chart",
    "features/gantt_chart",
    "features/crop_palette",
    "features/climate_chart",
    "features/home"
  ]) %>
  
  <!-- Bundled JavaScript (includes Leaflet) -->
  <%= render_common_javascripts(include_shared_systems: false) %>
</head>
<body>
  <!-- i18n data attributes (hidden) -->
  <div id="js-i18n" hidden <%= raw (js_i18n_data.merge(js_i18n_templates)).map { |k, v| "data-#{k.to_s.dasherize}=\"#{ERB::Util.html_escape(ActionView::Base.full_sanitizer.sanitize(v.to_s))}\"" }.join(' ') %>></div>
  <%= render "shared/navbar" %>

  <!-- Flash Messages -->
  <main class="<%= controller_name == 'home' && action_name == 'index' ? '' : 'container' %>">
    <%= render "shared/flash_messages" %>
    
    <%= yield %>
  </main>
  
  <%= render "shared/footer" %>
  
  <%= yield :javascripts %>
  
  <%# Development: Client-side logging to server %>
  <%# if Rails.env.development? %>
    <%#= javascript_include_tag "client_logger", "data-turbo-track": "reload", defer: true %>
  <%# end %>
</body>
</html>

