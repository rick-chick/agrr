<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
<head>
  <%= render "shared/meta_tags" %>
  
  <!-- Bundled CSS (Leaflet など npm パッケージ) - esbuild でビルド -->
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  
  <!-- Core Design System - 必ず最初に読み込む -->
  <%= render_core_stylesheets %>
  
  <!-- Utility CSS classes -->
  <%= render_utility_stylesheets %>
  
  <!-- Components -->
  <%= render_component_stylesheets(components: [
    "components/buttons",
    "components/forms",
    "components/navbar"
  ]) %>
  
  <!-- Auth-specific styles -->
  <%= render_feature_stylesheets(features: ["auth"]) %>
  
  <!-- Bundled JavaScript (includes Leaflet) -->
  <%= render_common_javascripts(include_shared_systems: false) %>
</head>
<body class="auth-body" <%= raw (js_i18n_data.merge(js_i18n_templates)).map { |k, v| "data-#{k.to_s.dasherize}=\"#{ERB::Util.html_escape(ActionView::Base.full_sanitizer.sanitize(v.to_s))}\"" }.join(' ') %>>
  <%= render "shared/navbar" %>
  
  <main class="container">
    <%= render "shared/flash_messages" %>
    
    <%= yield %>
  </main>
  
  <%= yield :javascripts %>
</body>
</html>

