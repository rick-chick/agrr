<% content_for :title, "#{t('interaction_rules.new.title')} - AGRR" %>

<div class="page-header">
  <h1 class="page-title"><%= t('interaction_rules.new.page_title') %></h1>
</div>

<div class="rule-detail-card">
  <%= render "form", interaction_rule: @interaction_rule %>
</div>
