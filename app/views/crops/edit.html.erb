<% content_for :title, "#{t('.title', name: @crop.name)} - AGRR" %>

<%# Stimulus に移行したため個別 JS 読み込みは不要 %>

<div class="page-header">
  <h1 class="page-title"><%= t('.title', name: @crop.name) %></h1>
</div>

<%= render 'form' %>
