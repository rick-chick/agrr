<% content_for :title, "農場一覧 - AGRR" %>

<div class="page-header">
  <h1 class="page-title">農場一覧</h1>
  <%= link_to "新しい農場を追加", new_farm_path, class: "btn btn-success" %>
</div>

<!-- デバッグ情報（開発環境のみ） -->
<% if Rails.env.development? %>
  <div style="background: #fff3cd; padding: 1rem; margin-bottom: 1rem; border-radius: 4px;">
    <strong>デバッグ:</strong>
    admin_user? = <%= admin_user? %>,
    current_user.admin = <%= current_user.admin %>,
    @reference_farms.count = <%= @reference_farms.count %>,
    @farms.count = <%= @farms.count %>
  </div>
<% end %>

<% if @reference_farms.any? %>
  <!-- 参照農場（管理者のみ表示） -->
  <div style="margin-bottom: 2rem;">
    <h2 style="font-size: 1.5rem; font-weight: 600; color: #2d3748; margin-bottom: 1rem;">参照農場（栽培地域） (<%= @reference_farms.count %>件)</h2>
    
    <div class="farms-grid">
      <% @reference_farms.each do |farm| %>
        <%= render "farm_card_wrapper", farm: farm %>
      <% end %>
    </div>
  </div>
  
  <hr style="margin: 2rem 0; border: none; border-top: 2px solid #e2e8f0;">
  
  <!-- 自分の農場 -->
  <h2 style="font-size: 1.5rem; font-weight: 600; color: #2d3748; margin-bottom: 1rem;">自分の農場 (<%= @farms.count %>件)</h2>
<% end %>
    
    <% if @farms.any? %>
      <div class="farms-grid" id="farms">
        <% @farms.each do |farm| %>
          <%= turbo_stream_from farm %>
          <%= render "farm_card_wrapper", farm: farm %>
        <% end %>
      </div>
    <% else %>
      <div class="empty-state">
        <div class="empty-state-icon">🚜</div>
        <h3>まだ農場が登録されていません</h3>
        <p>最初の農場を追加して始めましょう。</p>
        <%= link_to "農場を追加", new_farm_path, class: "btn btn-success" %>
      </div>
    <% end %>


