<% content_for :title, "農場一覧 - AGRR" %>

<div class="page-header">
  <h1 class="page-title">農場一覧</h1>
  <%= link_to "新しい農場を追加", new_farm_path, class: "btn btn-success" %>
</div>
    
    <% if @farms.any? %>
      <div class="fields-grid">
        <% @farms.each do |farm| %>
          <div class="field-card">
            <h3 class="field-name"><%= farm.display_name %></h3>
            <% if farm.has_coordinates? %>
              <div class="field-coordinates">
                📍 緯度: <%= farm.latitude %><br>
                📍 経度: <%= farm.longitude %>
              </div>
            <% else %>
              <div class="field-coordinates">📍 位置情報が設定されていません</div>
            <% end %>
            <div class="field-info">
              <p>圃場数: <%= farm.fields.count %>個</p>
            </div>
            <div class="field-actions">
              <%= link_to "詳細", farm, class: "btn btn-small" %>
              <%= link_to "圃場一覧", farm_fields_path(farm), class: "btn btn-small btn-info" %>
              <%= link_to "編集", edit_farm_path(farm), class: "btn btn-small btn-secondary" %>
              <%= button_to "削除", farm, method: :delete, 
                          class: "btn btn-small btn-danger", 
                          confirm: "この農場を削除しますか？" %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="empty-state">
        <div class="empty-state-icon">🌾</div>
        <h3>まだ農場が登録されていません</h3>
        <p>最初の農場を追加して始めましょう。</p>
        <%= link_to "農場を追加", new_farm_path, class: "btn btn-success" %>
      </div>
    <% end %>


