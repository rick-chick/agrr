<% content_for :title, "農場編集 - AGRR" %>

<div class="page-header">
  <h1 class="page-title">農場を編集</h1>
</div>
    
    <div class="form-card">
      <%= form_with model: @farm, local: true do |form| %>
        <% if @farm.errors.any? %>
          <div style="color: #dc3545; margin-bottom: 1rem;">
            <h4><%= pluralize(@farm.errors.count, "error") %> prohibited this farm from being saved:</h4>
            <ul>
              <% @farm.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
        
        <div class="form-group">
          <%= form.label :name, "農場名", class: "form-label" %>
          <%= form.text_field :name, class: "form-control", placeholder: "例: 田中農園" %>
          <div class="form-text">農場の名前を入力してください</div>
        </div>
        
        <div class="form-group">
          <label class="form-label">📍 位置情報</label>
          <div class="map-container">
            <div id="map"></div>
            <div class="map-placeholder" id="map-placeholder">
              <div>🗺️ 地図を読み込み中...</div>
            </div>
          </div>
          <div class="coordinates-input">
            <div>
              <%= form.label :latitude, "緯度", class: "form-label" %>
              <%= form.number_field :latitude, step: :any, class: "form-control", placeholder: "35.6762", id: "farm_latitude" %>
              <div class="form-text">例: 35.6762 (東京駅付近)</div>
            </div>
            <div>
              <%= form.label :longitude, "経度", class: "form-label" %>
              <%= form.number_field :longitude, step: :any, class: "form-control", placeholder: "139.6503", id: "farm_longitude" %>
              <div class="form-text">例: 139.6503 (東京駅付近)</div>
            </div>
          </div>
        </div>
        
        <div class="actions">
          <%= form.submit "更新", class: "btn" %>
          <%= link_to "キャンセル", @farm, class: "btn btn-secondary" %>
        </div>
      <% end %>
    </div>

