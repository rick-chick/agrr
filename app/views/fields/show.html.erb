<!DOCTYPE html>
<html>
<head>
  <title><%= @field.display_name %> - AGRR</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <%= stylesheet_link_tag "fields", "data-turbo-track": "reload" %>
</head>
<body>
  <div class="header">
    <div class="logo">AGRR</div>
    <div class="user-info">
      <a href="<%= root_path %>" class="nav-link">ホーム</a>
      <%= link_to "農場", farms_path, class: "nav-link" %>
      <% if current_user.avatar_url.present? %>
        <%= image_tag current_user.avatar_url, alt: "User Avatar", class: "user-avatar" %>
      <% end %>
      <span><%= current_user.name %></span>
      <%= button_to "ログアウト", auth_logout_path, method: :delete, class: "logout-btn" %>
    </div>
  </div>
  
  <div class="container">
    <% if flash[:notice] %>
      <div class="alert"><%= flash[:notice] %></div>
    <% end %>
    
    <div class="breadcrumb">
      <%= link_to "農場一覧", farms_path, class: "breadcrumb-link" %>
      <span class="breadcrumb-separator">></span>
      <%= link_to @farm.display_name, farm_path(@farm), class: "breadcrumb-link" %>
      <span class="breadcrumb-separator">></span>
      <%= link_to "圃場一覧", farm_fields_path(@farm), class: "breadcrumb-link" %>
      <span class="breadcrumb-separator">></span>
      <span class="breadcrumb-current"><%= @field.display_name %></span>
    </div>
    
    <div class="page-header">
      <h1 class="page-title"><%= @field.display_name %></h1>
      <div>
        <%= link_to "編集", edit_farm_field_path(@farm, @field), class: "btn btn-secondary" %>
      </div>
    </div>
    
    <div class="field-card">
      <div class="field-info">
        <div class="info-item">
          <span class="info-label">📝 名前:</span>
          <span class="info-value"><%= @field.name %></span>
        </div>
        
        
        <div class="info-item">
          <span class="info-label">📅 作成日:</span>
          <span class="info-value"><%= @field.created_at.strftime("%Y年%m月%d日 %H:%M") %></span>
        </div>
        
        <div class="info-item">
          <span class="info-label">📅 更新日:</span>
          <span class="info-value"><%= @field.updated_at.strftime("%Y年%m月%d日 %H:%M") %></span>
        </div>
      </div>
      
      <div class="actions">
        <%= link_to "圃場一覧に戻る", farm_fields_path(@farm), class: "btn" %>
        <%= link_to "編集", edit_farm_field_path(@farm, @field), class: "btn btn-secondary" %>
        <%= button_to "削除", farm_field_path(@farm, @field), method: :delete, 
                    class: "btn btn-danger", 
                    confirm: "この圃場を削除しますか？" %>
      </div>
    </div>
  </div>
</body>
</html>