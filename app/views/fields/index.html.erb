<!DOCTYPE html>
<html>
<head>
  <title><%= @farm.display_name %> - 圃場一覧 - AGRR</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <%= stylesheet_link_tag "fields", "data-turbo-track": "reload" %>
</head>
<body>
  <div class="header">
    <div class="logo">AGRR</div>
    <div class="user-info">
      <a href="<%= root_path %>" class="nav-link">ホーム</a>
      <%= link_to "農場", farms_path, class: "nav-link" %>
      <% if current_user.avatar_url.present? %>
        <%= image_tag current_user.avatar_url, alt: "User Avatar", class: "user-avatar" %>
      <% end %>
      <span><%= current_user.name %></span>
      <%= button_to "ログアウト", auth_logout_path, method: :delete, class: "logout-btn" %>
    </div>
  </div>
  
  <div class="container">
    <% if flash[:notice] %>
      <div class="alert"><%= flash[:notice] %></div>
    <% end %>
    
    <div class="breadcrumb">
      <%= link_to "農場一覧", farms_path, class: "breadcrumb-link" %>
      <span class="breadcrumb-separator">></span>
      <%= link_to @farm.display_name, farm_path(@farm), class: "breadcrumb-link" %>
      <span class="breadcrumb-separator">></span>
      <span class="breadcrumb-current">圃場一覧</span>
    </div>
    
    <div class="page-header">
      <h1 class="page-title"><%= @farm.display_name %> - 圃場一覧</h1>
      <%= link_to "新しい圃場を追加", new_farm_field_path(@farm), class: "btn btn-success" %>
    </div>
    
    <% if @fields.any? %>
      <div class="fields-grid">
        <% @fields.each do |field| %>
          <div class="field-card">
            <h3 class="field-name"><%= field.display_name %></h3>
            <div class="field-actions">
              <%= link_to "詳細", farm_field_path(@farm, field), class: "btn btn-small" %>
              <%= link_to "編集", edit_farm_field_path(@farm, field), class: "btn btn-small btn-secondary" %>
              <%= button_to "削除", farm_field_path(@farm, field), method: :delete, 
                          class: "btn btn-small btn-danger", 
                          confirm: "この圃場を削除しますか？" %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="empty-state">
        <div class="empty-state-icon">🌾</div>
        <h3>まだ圃場が登録されていません</h3>
        <p>最初の圃場を追加して始めましょう。</p>
        <%= link_to "圃場を追加", new_farm_field_path(@farm), class: "btn btn-success" %>
      </div>
    <% end %>
  </div>
</body>
</html>