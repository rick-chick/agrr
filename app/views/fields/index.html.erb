<!DOCTYPE html>
<html>
<head>
  <title>圃場一覧 - AGRR</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <%= stylesheet_link_tag "fields", "data-turbo-track": "reload" %>
</head>
<body>
  <div class="header">
    <div class="logo">AGRR</div>
    <div class="user-info">
      <a href="<%= root_path %>" class="nav-link">ホーム</a>
      <%= link_to "圃場", fields_path, class: "nav-link" %>
      <% if current_user.avatar_url.present? %>
        <img src="<%= current_user.avatar_url %>" alt="User Avatar" class="user-avatar">
      <% end %>
      <span><%= current_user.name %></span>
      <%= button_to "ログアウト", auth_logout_path, method: :delete, class: "logout-btn" %>
    </div>
  </div>
  
  <div class="container">
    <% if flash[:notice] %>
      <div class="alert"><%= flash[:notice] %></div>
    <% end %>
    
    <div class="page-header">
      <h1 class="page-title">圃場一覧</h1>
      <%= link_to "新しい圃場を追加", new_field_path, class: "btn btn-success" %>
    </div>
    
    <% if @fields.any? %>
      <div class="fields-grid">
        <% @fields.each do |field| %>
          <div class="field-card">
            <h3 class="field-name"><%= field.display_name %></h3>
            <% if field.has_coordinates? %>
              <div class="field-coordinates">
                📍 緯度: <%= field.latitude %><br>
                📍 経度: <%= field.longitude %>
              </div>
            <% else %>
              <div class="field-coordinates">📍 位置情報が設定されていません</div>
            <% end %>
            <div class="field-actions">
              <%= link_to "詳細", field, class: "btn btn-small" %>
              <%= link_to "編集", edit_field_path(field), class: "btn btn-small btn-secondary" %>
              <%= button_to "削除", field, method: :delete, 
                          class: "btn btn-small btn-danger", 
                          confirm: "この圃場を削除しますか？" %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="empty-state">
        <div class="empty-state-icon">🌾</div>
        <h3>まだ圃場が登録されていません</h3>
        <p>最初の圃場を追加して始めましょう。</p>
        <%= link_to "圃場を追加", new_field_path, class: "btn btn-success" %>
      </div>
    <% end %>
  </div>
</body>
</html>