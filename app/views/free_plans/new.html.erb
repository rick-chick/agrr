<%# encoding: UTF-8 %>
<div class="free-plans-wrapper">
  <div class="free-plans-container">
    <!-- Header -->
    <div class="free-plans-header">
      <h1>🌱 作付け計画作成</h1>
      <p>地域、農場サイズ、作物を選んで作付け計画を作成しましょう</p>
    </div>
    
    <!-- Progress Steps Card -->
    <div class="progress-steps-card">
      <div class="enhanced-progress-steps">
        <div class="enhanced-step-item active">
          <div class="enhanced-step-circle">1</div>
          <span class="enhanced-step-label">地域選択</span>
        </div>
        <div class="enhanced-step-item">
          <div class="enhanced-step-circle">2</div>
          <span class="enhanced-step-label">農場サイズ</span>
        </div>
        <div class="enhanced-step-item">
          <div class="enhanced-step-circle">3</div>
          <span class="enhanced-step-label">作物選択</span>
        </div>
      </div>
    </div>
    
    <!-- Content Card -->
    <div class="content-card">
      <h2 class="content-card-title">🌍 栽培地域を選択してください</h2>
      <p class="content-card-subtitle">あなたの農場がある地域を選択してください</p>
      
      <div class="enhanced-grid">
        <% @farms.each do |farm| %>
          <%= link_to select_farm_size_free_plans_path(farm_id: farm.id), class: "enhanced-selection-card" do %>
            <div class="enhanced-card-icon">🌏</div>
            <div class="enhanced-card-title"><%= farm.name %></div>
            <div class="enhanced-card-subtitle">
              <%= "#{farm.latitude.round(2)}, #{farm.longitude.round(2)}" %>
            </div>
          <% end %>
        <% end %>
      </div>
      
      <% if @farms.empty? %>
        <div class="empty-selection">
          <div class="empty-selection-icon">🌍</div>
          <p>現在、選択可能な栽培地域がありません。</p>
        </div>
      <% end %>
    </div>
  </div>
</div>
