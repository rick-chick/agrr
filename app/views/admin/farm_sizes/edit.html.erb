<% content_for :title, "Editing farm size" %>

<h1>Editing farm size</h1>

<%= render "form", farm_size: @farm_size %>

<br>

<div>
  <%= link_to "Show this farm size", [:admin, @farm_size] %> |
  <%= link_to "Back to farm sizes", admin_farm_sizes_path %>
</div>
