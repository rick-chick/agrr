<%= form_with(model: [:admin, farm_size]) do |form| %>
  <% if farm_size.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(farm_size.errors.count, "error") %> prohibited this farm_size from being saved:</h2>

      <ul>
        <% farm_size.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :area_sqm, style: "display: block" %>
    <%= form.number_field :area_sqm %>
  </div>

  <div>
    <%= form.label :display_order, style: "display: block" %>
    <%= form.number_field :display_order %>
  </div>

  <div>
    <%= form.label :active, style: "display: block" %>
    <%= form.checkbox :active %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
