# SQLite. Versions 3.8.0 and up are supported.
# With Rails 8 and proper configuration, SQLite is production-ready.

default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  # Busy timeout for SQLite in milliseconds - increase in production to reduce
  # "database is locked" errors when multiple processes access DB files.
  timeout: <%= ENV.fetch("SQLITE_BUSY_TIMEOUT_MS", 20000) %>

development:
  primary:
    <<: *default
    database: storage/development.sqlite3
  queue:
    <<: *default
    database: storage/development_queue.sqlite3
    migrations_paths: db/queue_migrate
  cache:
    <<: *default
    database: storage/development_cache.sqlite3
    migrations_paths: db/cache_migrate
  cable:
    <<: *default
    database: storage/development_cable.sqlite3
    migrations_paths: db/cable_migrate
    schema_dump: cable_schema.rb

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
# 
# IMPORTANT: Tests should be run in isolated test container with separate storage volume.
# Use: docker compose run --rm test bundle exec rails test
test:
  primary:
    <<: *default
    database: storage/test.sqlite3
    # システムテストでのデータベース共有のためWALモードを有効化
    # journal_mode: walを設定すると、複数の接続でデータを共有できる
    # prepared_statements: falseで、同一接続の再利用を防ぐ
  queue:
    <<: *default
    database: storage/test_queue.sqlite3
    migrations_paths: db/queue_migrate
  cache:
    <<: *default
    database: storage/test_cache.sqlite3
    migrations_paths: db/cache_migrate
  cable:
    <<: *default
    database: storage/test_cable.sqlite3
    migrations_paths: db/cable_migrate
    schema_dump: cable_schema.rb

# AWS Test environment (SQLite with persistent storage)
aws_test:
  <<: *default
  database: storage/aws_test.sqlite3

# Cloud Run Production environment (SQLite with separate files for better performance)
production:
  primary:
    <<: *default
    database: /tmp/production.sqlite3
  queue:
    <<: *default
    database: /tmp/production_queue.sqlite3
    migrations_paths: db/queue_migrate
    # Use WAL journal mode to reduce writer/reader contention
    journal_mode: wal
  cache:
    <<: *default
    database: /tmp/production_cache.sqlite3
    migrations_paths: db/cache_migrate
  cable:
    <<: *default
    database: /tmp/production_cable.sqlite3
    migrations_paths: db/cable_migrate
    schema_dump: cable_schema.rb
    # Use WAL for cable DB to reduce locking during concurrent access
    journal_mode: wal
