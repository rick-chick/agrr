# Litestream configuration for Cloud Storage replication
# Single database for simplicity (includes all tables: primary, queue, cache)
dbs:
  - path: /tmp/production.sqlite3
    replicas:
      - type: gcs
        bucket: ${GCS_BUCKET}
        path: production.sqlite3
        sync-interval: 10s

