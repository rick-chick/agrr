{
  "name": "app",
  "private": true,
  "scripts": {
    "build": "npx esbuild app/javascript/application.js --bundle --sourcemap --format=esm --outdir=app/assets/builds --public-path=/assets --external:*.png --external:*.jpg --external:*.jpeg --external:*.gif --external:*.svg"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo-rails": "^8.0.18",
    "chart.js": "^4.5.0",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  }
}
