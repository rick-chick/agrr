# P6 BC 切替進捗 — 終着: Rails 廃止・API フォールバックなし
# 履歴（P6 完了済み）。正: ../app-rust-stack/P8-RAILS-SHELL-REMOVAL.md, ../lib-domain-rust/TEST-STRATEGY.md
# 正: P6-COMPLETION-CRITERIA.md, PRODUCTION-CUTOVER-STATUS.md
#
# P8 (2026-06-02): wave_e の Ruby 契約は P6 時点の R4 ゲート。API は agrr-server 実装済み。
# auth_me / auth_logout は agrr-r4-contract への移植対象外 — P8-RAILS-SHELL-REMOVAL.md「Ruby 契約の P8.6 扱い」

program: app-rust-stack-p6
updated: 2026-05-30
policy:
  rails_api_fallback: forbidden
  unimplemented_api: rust_501_api_not_migrated
  dev_stack: .cursor/skills/dev-docker/scripts/host-rust-stack.sh

wave_e:
  contract_tests:
    - auth_me_contract_test.rb       # P8.6: drop without Rust port (auth_api.rs + E2E)
    - auth_logout_contract_test.rb   # P8.6: drop without Rust port
    - internal_jobs_contract_test.rb
    - private_plan_optimization_job_chain_contract_test.rb
    - field_cultivation_patch_contract_test.rb
    - plan_adjust_db_sync_contract_test.rb
    - deletion_undo_restore_contract_test.rb
    - public_plan_save_contract_test.rb
    - backdoor_api_contract_test.rb
    - masters_farms_contract_test.rb
    - masters_crops_contract_test.rb
    - masters_patch_contract_test.rb
    - masters_crop_nested_contract_test.rb
    - public_plans_api_contract_test.rb
    - public_plan_cultivation_mutations_contract_test.rb
    - internal_farm_weather_contract_test.rb
    - crates/agrr-r4-contract/tests/contracts.rs  # health, /cable (P8: AI smokes removed — domain)
  nginx_checklist: docker/nginx-strangler-host.conf
  rust_contract_runner: scripts/run-rust-contract-tests.sh
  angular_routing_audit: scripts/run-rust-contract-tests.sh

critical_path:
  - id: auth
    phase: done
    notes: "Rust OAuth + auth_api; AGRR_RUST_API gates Rails routes; nginx /auth + /api/v1/auth/*"
  - id: websocket_jobs
    phase: done
    notes: "In-process optimization chain + shared CableHub broadcast; stub removed"
  - id: cultivation_plan
    phase: done
    notes: "Mutations + reads on Rust; FieldCultivationSync after adjust (sqlite gateways)"
  - id: internal_jobs
    phase: done
    notes: "trigger_weather_update via SchedulerWeatherBatchEnqueueInteractor + FetchWeatherDataPerformInteractor (farm_weather_fetch)"

waves:
  wave-2-remaining:
    - { context: api_keys, phase: done }
    - { context: backdoor, phase: done, notes: "7 routes on Rust; clear_db via Interactor; status/users/db_stats edge→sqlite gateway (Rails parity); Rails gated by AGRR_RUST_API" }
    - { context: contact_messages, phase: done }
  wave-3:
    - { context: deletion_undo, phase: done }
    - { context: interaction_rule, phase: done }
    - { context: public_plan, phase: done, notes: "wizard + save_plan + adjust on Rust" }
    - { context: field, phase: done }
    - { context: agricultural_task, phase: done }
    - { context: fertilize, phase: done }
    - { context: pesticide, phase: done }
    - { context: farm, phase: done }
    - { context: pest, phase: done }
  wave-4:
    - { context: field_cultivation, phase: done, notes: "show/PATCH/climate GET; patch contract" }
    - { context: weather_data, phase: done, notes: "ADC read/list + Result propagation; fetch/scheduler/internal farm read/field climate via WeatherDataGatewayBundle; startup validate_weather_storage_config; review gaps (a)(b)(c); plan_allocation_adjust_read_gateway SQLite weather_data = separate task; see WEATHER-DATA-GCS-SMOKE.md" }
    - { context: crop, phase: done }
