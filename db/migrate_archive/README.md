# Archived Rails migrations (P7)

Historical `db/migrate` files (schema + reference data). **New schema changes** use `crates/agrr-migrate/migrations/schema/` (refinery).

Reference data re-apply: `agrr-migrate data apply` (see [P7-MIGRATION-RUNBOOK.md](../../docs/migration/app-rust-stack/P7-MIGRATION-RUNBOOK.md)).
