/**
 * When true, the cookie UI is not shown and analytics consent is auto-granted
 * (see cookie-consent-banner). GA consent defaults must stay aligned with this.
 */
export const COOKIE_CONTROL_UI_DISABLED = true;
