# Minimal URL map: all HTTP requests redirect to HTTPS (301).
# Import with scripts/apply-apex-http-to-https-redirect.sh
name: agrr-http-to-https-redirect
defaultUrlRedirect:
  httpsRedirect: true
  redirectResponseCode: MOVED_PERMANENTLY_DEFAULT
  stripQuery: false
